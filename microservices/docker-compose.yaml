
services:
  mongodb:
    image: mongo:latest
    container_name: doctor_db_container
    ports:
      - "27017:27017" 
    volumes:
      - mongo_data:/data/db

networks:
  app-network:
    driver: bridge

volumes:
  mongo_data: